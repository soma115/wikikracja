from django.shortcuts import render
from .models import Customize

# def view(request):
#     start = Customize.objects.get(title='Footer')
#     return render(request, 'customize/footer.html', {'start': start,})
